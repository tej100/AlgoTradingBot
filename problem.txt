Implement a RandomForest model-based trading strategy following the class structure, method signatures and conventions of the existing Ridge_Indicator strategy. Integrate it with the existing strategy executer class and frontend so it is selectable alongside existing strategies. Additionally, create a backtesting file that runs all existing strategies plus the new strategy using ticker BTC-USD at the 5m interval with max data period and position='both'. The backtester must use walk-forward validation — retraining the model on each new window rather than fitting once on all historical data. All strategies must be tested on identical date ranges to ensure fair comparison. Do not use any external backtesting libraries — calculate all performance metrics manually, including Sharpe ratio, Sortino Ratio, max drawdown, total return, alpha/beta relative to buy&hold, and win rate. When encountering missing data or time series gaps, forward-fill missing values; skip any training windows with insufficient data points rather than crashing.